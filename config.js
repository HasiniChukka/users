const config = {
    db: {
      host: "localhost",
      user: "root",
      password: "@hasini2003",
      database: "local_jobs",
      connectTimeout: 60000
    },
    listPerPage: 10,
  };
  module.exports = config;
